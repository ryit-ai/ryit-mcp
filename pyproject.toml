[project]
name = "ryit-mcp"
version = "0.3.0"
description = "MCP server for syncing project documentation to ryit.ai"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0.0",
    "httpx>=0.27.0",
    "python-frontmatter>=1.0",
]

[project.scripts]
ryit-mcp = "server:mcp.run"
